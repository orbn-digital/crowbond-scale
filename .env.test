# Test Environment Configuration
NODE_ENV=test
LOG_LEVEL=error

# Ably Configuration (use test credentials)
ABLY_API_KEY=test_key_for_testing

# New Relic Configuration (use test credentials)
NEW_RELIC_LICENSE_KEY=test_license_key
NEW_RELIC_APP_NAME=Crowbond-Scales-Test
NEW_RELIC_LOG_LEVEL=error

# Scale Configuration
SCALE_IPS=127.0.0.1
LOCAL_PORT=7777
REMOTE_PORT=7778

# Service Configuration
SERVICE_PORT=3001
SCALE_TIMEOUT=1000
RETRY_ATTEMPTS=1
RETRY_DELAY=100
HEALTH_CHECK_INTERVAL=60000